function multiply(number){
  return number * Math.pow(5, Math.abs(number).toString().length)
}
// Math.pow() base raised to exponent, can try **
// Math.abs() returns absolute value
// .toString() return as string
