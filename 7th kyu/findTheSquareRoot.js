function squareRoot(x) {
  return +(x**0.5).toFixed(5)
}

// Alternative to Math.pow(), Math.sqrt()
// +var returns integer
// ** Exponentiation similar to Math.pow()
// .toFixed() formats number to fixed point notation
