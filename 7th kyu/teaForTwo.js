function tea42(input) {
  return input.toString().replace (/2/g, 't')
};

// .toString() returns string
// .replace() replaces first instance of string with replacement
// "/ /g" REGEX replaces all instances of
