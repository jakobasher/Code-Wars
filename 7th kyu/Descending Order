function descendingOrder(n){
  return Number(n.toString().split('').sort((a,b)=>b-a).join(''))

}

// Number
// .toString() returns string
// .split("") string into ordered list
// .sort() try .reverse() next time
// .join("") concat array elements into string
