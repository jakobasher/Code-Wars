function betweenExtremes(numbers) {
    return Math.max(...numbers) - Math.min(...numbers);
}
// Math.max() returns max value among
// Math.min() returns min value among
// ... Spready syntax array to iterible list
