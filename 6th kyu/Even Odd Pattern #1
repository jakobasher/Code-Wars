function EvenOdd(arr){
  let ans = 0
  
  for (let i = 0; i<arr.length; i++) {
    if (i%2 == 0) {
      ans += arr[i]
    } else {
      ans *= arr[i]
    }
  }
  
  return ans
  
}

// Can also use arr.reduce(a,c,i=> ) next time
