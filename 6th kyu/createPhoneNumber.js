function createPhoneNumber(numbers){
  let num1 = "("+numbers[0]+numbers[1]+numbers[2]
  let num2 = ") "+numbers[3]+numbers[4]+numbers[5]+"-"+numbers[6]+numbers[7]+numbers[8]+numbers[9]
  
  return num1+num2
  
}

// try for loop .replace()
// try .join() and .substring()
